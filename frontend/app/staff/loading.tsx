import { RouteLoadingState } from "@/components/ui/RouteLoadingState";

export default function StaffLoading() {
  return <RouteLoadingState label="Staff portal" title="Loading commissary operations..." />;
}
